<%= form_for(@my_book) do |f| %>
  <% if @my_book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@my_book.errors.count, "error") %> prohibited this my_book from being saved:</h2>

      <ul>
      <% @my_book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :book %><br>
    <%= f.text_field :book %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
